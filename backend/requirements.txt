# Note: This file is a convenience copy. If using Render with
# Root Directory = "backend", you must also set PYTHONPATH=..
# to allow imports from agents/, schemas/, simulation/, stadium/.
# Recommended: leave Root Directory empty and use ../requirements.txt.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
PyYAML>=6.0
google-genai>=1.0.0
